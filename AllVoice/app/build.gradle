apply plugin: 'com.android.application'

android {
    signingConfigs {
        AllVoice {
            keyAlias 'AllVoiceAndroidKey'
            keyPassword 'mauldin'
            storeFile file('C:/Users/Peter.Peter-Desktop/.android/allvoice.jks')
            storePassword 'mauldin'
        }
    }
    compileSdkVersion 21
    buildToolsVersion "21.1.2"
    defaultConfig {
        applicationId "me.pmauldin.allvoice"
        minSdkVersion 14
        targetSdkVersion 21
        versionCode 1
        versionName "1.0"
    }
    buildTypes {
                AllVoice {
            signingConfig signingConfigs.AllVoice
        }
    }
    productFlavors {
    }
}

dependencies {
    compile files('libs/dropbox-android-sdk-1.6.2.jar')
    compile project(':json_simple-1.1')
    compile fileTree(include: ['*.jar'], dir: 'libs')
    compile 'com.android.support:appcompat-v7:21.0.3'
    compile 'com.google.android.gms:play-services:6.5.87'
    compile 'com.android.support:support-v4:21.0.3'
    compile project(':json_simple-1.1')
    compile project(':src')
}
